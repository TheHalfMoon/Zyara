# PR Instructions

Open a pull request from `plan/zyara-v1-discovery-ai-direction` to `main`, validate any docs/CI checks, and merge only if clean. The packet itself is planning input, not implementation evidence.