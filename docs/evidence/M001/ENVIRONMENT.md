# M001 Environment

Date (UTC): 2026-09-15. Host: Windows 11, Docker Desktop 29.7.2.

| Component | Version |
|---|---|
| Node.js | v22.23.1 (engines: >=22.12.0) |
| pnpm | 9.12.0 |
| TypeScript | 5.6.3 |
| ESLint (+typescript-eslint) | 9.14.0 (+8.12.0) |
| Next.js | 15.5.25 |
| React / React DOM | 19.3.0 |
| Fastify | 5.12.4 |
| pg | 8.13.1 |
| tsx | 4.19.2 |
| @types/node | 22.9.0 |
| Dev DB image (compose, pinned) | postgis/postgis:16-3.4 |
| Dev DB credentials | zyara_dev / zyara_dev_only (non-production, local-only) |
| Dev ports | web 3100, api 4100, postgres 5433->5432 |

Lockfile: `pnpm-lock.yaml`, SHA256 6533FCA666749E70B36D87885E4E7322D96830AD78D883B1BA4B817348F2AED2
(at evidence time; regenerated by dependency upgrades — final hash rebound at commit).

## Local setup (clean checkout)

```bash
pnpm install --frozen-lockfile
docker compose -f infra/dev/docker-compose.yml up -d
pnpm dev
```

- API: http://localhost:4100/live (liveness), http://localhost:4100/ready (readiness)
- Web: http://localhost:3100
- Stop/reset: `docker compose -f infra/dev/docker-compose.yml down`
  (add `-v` to drop the dev volume; dev data only, never PHI).

## Environment incidents during M001

1. Disk C: was 100% full. Freed ~10 GB by deleting stale third-party temp
   directories under `%LOCALAPPDATA%\Temp\opencode` (medscale-*, commandF).
2. Docker Desktop daemon storage is corrupted
   (`.../io.containerd.metadata.v1.bolt/meta.db: read-only file system`,
   later I/O errors) — containers cannot start on this host.
   Mitigation for M001 runtime proof: real PostgreSQL 18.4 via embedded binary
   on port 5433/5434 with the same dev credentials. Canonical compose file keeps
   the pinned `postgis/postgis:16-3.4` image; CI (Ubuntu) exercises compose.
